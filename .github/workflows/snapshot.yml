name: Release

on:
  push:
    branches:
      - "main"
      - "mc/*"

jobs:
  build:
    if: |
      !contains(github.event.head_commit.message, '[ciskip]')
    uses: nanite/workflows/.github/workflows/release.yml@v1.3
    with:
      java-version: 25
      gradle-tasks: build publish  --no-daemon
    secrets:
      nanite-token: ${{ secrets.NANITE_DEPLOY }}
